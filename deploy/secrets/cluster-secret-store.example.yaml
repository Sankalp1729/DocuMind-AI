apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: documind-secret-store
spec:
  provider:
    kubernetes:
      remoteNamespace: default
      auth:
        serviceAccount:
          name: external-secrets
          namespace: external-secrets
